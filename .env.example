# Optional: only for later bulk machine draft (NOT required for Cursor Agent gold work)
# Prefer: ask Cursor Agent to restyle sutras in-chat → scripts/apply_gold_*.py
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-chat

# Pilot size when fetching
PILOT_COUNT=10
